{
  "name": "db",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "bash -c 'docker compose up'",
    "dbmate:up": "dbmate up --strict",
    "test": "dbmate up --strict --env-file .env.test",
    "check-updates": "npm-check-updates -t minor"
  },
  "dependencies": {
    "dbmate": "^2.24.2"
  }
}